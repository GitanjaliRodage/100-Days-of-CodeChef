# Declare a string with a single product code
product_code = "prod-12345"

# Check if the product code starts with "prod-" and print the result
is_valid = product_code.startswith("prod-")
print(is_valid)
